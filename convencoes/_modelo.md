<!-- Modelo. Copie para <nome-exato-do-repo>.md (ex: API-Producer-NODE.md).
     Este arquivo inteiro entra no contexto na primeira escrita de codigo da
     sessao — por isso: poucas regras, todas verificaveis, todas confirmadas
     pelo Gabriel. Regra sem caminho de arquivo nao entra. -->

## Padroes confirmados

- **Erros**: sempre via `<Classe>` (`caminho/do/arquivo`). Nunca `throw` cru.
- **Datas**: so por `caminho/do/helper`. Nao adicionar outra lib de data.
- **Resposta de API**: formato de `caminho/do/arquivo`.

## Nao fazer aqui

- Nao criar `utils/` novo: o projeto usa `<pasta real>`.

## Superados

> ⚠️ **Superado em AAAA-MM-DD**: <o que mudou e por que>
